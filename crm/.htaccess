<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /crm/

    # URL rewriting for CRM sub-application
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php?/$1 [L]
</IfModule>
